spring.application.name=myfirsttodoapp
#server.port=8081
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
logging.level.com.firsttodo.springboot.myfirsttodoapp.login=INFO
spring.mvc.format.date=dd-MM-yyyy
#spring.datasource.url=jdbc:h2:mem:testdb
spring.jpa.defer-datasource-initialization=true
spring.datasource.url=jdbc:postgresql://localhost:5432/todos
spring.datasource.username=todouser
spring.datasource.password=todopassword
spring.jpa.properties..hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update


#services:
#  postgres:
#    image: postgres:16
#    container_name: todo-postgres
#    environment:
#      POSTGRES_USER: todouser
#      POSTGRES_PASSWORD: todopassword
#      POSTGRES_DB: todos
#    ports:
#      - "5432:5432"
#    volumes:
#      - postgres-data:/var/lib/postgresql/data
#    restart: unless-stopped
#    healthcheck:
#      test: ["CMD-SHELL", "pg_isready -U todouser -d todos"]
#      interval: 10s
#      timeout: 5s
#      retries: 5

#volumes:
#  postgres-data:
#    driver: local